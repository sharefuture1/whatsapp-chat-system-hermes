# 前端独立部署配置示例
#
# 复制为 .env（或 .env.local）后在本地开发时生效；
# 部署到 Vercel / Netlify / 任意静态托管时，请在平台的
# 「环境变量」设置里配置同名变量（Vite 只读取以 VITE_ 开头的变量）。

# 后端 API 的地址，必须包含路径前缀 /api。
#
# 权威变量名（SDD VCL-002）：VITE_API_BASE_URL
# 留空时前端使用相对路径 /api，要求静态托管侧配置了到后端的同源反向代理，
# 或后端以单进程模式同时托管前端。
VITE_API_BASE_URL=https://your-backend.example.com/api

# 本地开发示例：
# VITE_API_BASE_URL=http://127.0.0.1:8792/api

# ── 兼容说明 ──────────────────────────────────────────────────────────
# 历史变量名 VITE_API_BASE 仍被识别（见 web/src/api.js 的 resolveApiBase），
# 但会在浏览器控制台打印弃用告警。新部署请统一使用 VITE_API_BASE_URL。
#
# 引用该变量的既有文档：docs/TAURI2.md（桌面壳打包）
