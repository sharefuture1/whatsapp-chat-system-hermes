# Public build configuration only. Never add credentials to VITE_* variables.
#
# 权威变量名见 SDD VCL-002（前端直连自托管 API）。
# 历史名 VITE_API_BASE 仍被识别，但会在控制台打印弃用告警。
VITE_API_BASE_URL=https://whats.future1.us/api
