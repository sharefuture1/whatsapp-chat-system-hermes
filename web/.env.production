# Public browser production defaults intentionally stay empty.
# Vercel Production injects the reviewed public API base in vite.config.js.
# Vercel Preview receives no production fallback (VCL-005).
# Never add credentials to VITE_* variables.
